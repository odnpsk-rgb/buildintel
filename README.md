# 🏗️ BuildIntel - AI Ассистент для строительства

Приложение для анализа маркетинга в строительстве с поддержкой мультимодальности (текст и изображения).

![Python](https://img.shields.io/badge/Python-3.9+-blue.svg)
![FastAPI](https://img.shields.io/badge/FastAPI-0.104+-green.svg)
![OpenAI](https://img.shields.io/badge/OpenAI-GPT--4o-purple.svg)

## 📋 Описание

Приложение доступно в двух вариантах:
- **Веб-версия** — браузерный интерфейс на http://localhost:8000
- **Десктопное приложение** — PyQt5 приложение (можно собрать в .exe файл)

Возможности:
- **Анализ продающих текстов в строительстве** — получать структурированную маркетинговую аналитику с сильными/слабыми сторонами, уникальными предложениями и рекомендациями
- **Анализ планировок квартир** — оценка удобства планировок, анализ расположения комнат, санузлов, лифтов, сильные и слабые стороны
- **Парсинг сайтов** — автоматически извлекать и анализировать контент по URL (использует Selenium)
- **Хранить историю** — последние 10 запросов сохраняются для быстрого доступа

## 🚀 Быстрый старт

### 1. Клонирование и установка зависимостей

```bash
# Перейдите в директорию проекта
cd monitor_kp

# Создайте виртуальное окружение
python -m venv venv

# Активируйте окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini
OPENAI_VISION_MODEL=gpt-4o-mini
```

### 3. Запуск приложения

**Вариант А: Веб-версия**

```bash
# Запуск сервера
python -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
# Или используйте скрипт:
start.bat  # Windows
```

Приложение будет доступно по адресу: http://localhost:8000

**Вариант Б: Десктопное приложение**

1. Установите зависимости PyQt5:
```bash
cd buildintel_app
pip install -r requirements.txt
```

2. Запустите приложение:
```bash
# Дважды кликните на ЗАПУСТИТЬ.bat
# Или через терминал:
python main.py
```

3. Для создания .exe файла:
```bash
# Дважды кликните на build_exe_advanced.bat
# Или через терминал:
.\build_exe_advanced.bat
```

Подробнее о десктопном приложении: см. `buildintel_app/README.md`

## 📁 Структура проекта

```
monitor_kp/
├── backend/                     # Backend модуль (FastAPI)
│   ├── __init__.py
│   ├── main.py                  # FastAPI приложение
│   ├── config.py                # Конфигурация
│   ├── models/
│   │   ├── __init__.py
│   │   └── schemas.py           # Pydantic модели
│   └── services/
│       ├── __init__.py
│       ├── openai_service.py   # Работа с OpenAI API
│       ├── parser_service.py    # Парсинг веб-страниц (Selenium)
│       └── history_service.py   # Управление историей
├── frontend/                     # Frontend модуль (веб-интерфейс)
│   ├── index.html               # HTML страница
│   ├── styles.css               # Стили
│   ├── app.js                   # JavaScript логика
│   └── 123.jpg                  # Фоновое изображение
├── buildintel_app/              # Десктопное приложение (PyQt5)
│   ├── main.py                  # Точка входа приложения
│   ├── api_client.py            # Клиент для работы с API
│   ├── ui/                      # UI модули
│   │   ├── main_window.py       # Главное окно
│   │   ├── text_tab.py          # Вкладка анализа текста
│   │   ├── image_tab.py         # Вкладка анализа планировок
│   │   ├── parse_tab.py         # Вкладка парсинга
│   │   └── history_tab.py        # Вкладка истории
│   ├── requirements.txt         # Зависимости PyQt5 приложения
│   ├── build_exe_advanced.bat   # Создание .exe файла
│   ├── ЗАПУСТИТЬ.bat            # Запуск приложения
│   └── README.md                # Документация десктопного приложения
├── chromedriver-win64/          # ChromeDriver для парсинга
│   └── chromedriver.exe
├── requirements.txt             # Зависимости Python (бэкенд)
├── start.bat                    # Скрипт запуска сервера (Windows)
├── start.sh                     # Скрипт запуска сервера (Linux/Mac)
├── .env                         # Переменные окружения (создаётся вручную)
├── history.json                 # Файл истории (создаётся автоматически)
├── README.md                    # Этот файл
└── docs.md                      # Документация API
```

## 🔧 Функциональность

### Анализ продающего текста в строительстве (`/analyze_text`)
- Принимает продающий текст конкурента в строительной сфере (минимум 10 символов)
- Возвращает маркетинговый анализ:
  - Сильные стороны текста (эффективность формулировок, использование триггеров, социальное доказательство)
  - Слабые стороны текста (что можно улучшить для повышения конверсии)
  - Уникальные предложения и УТП (конкурентные преимущества, выгоды для клиента)
  - Рекомендации по улучшению продающего текста (конкретные советы для строительного маркетинга)
  - Резюме маркетинговой эффективности текста

### Анализ планировок квартир (`/analyze_image`)
- Принимает изображения планировок: PNG, JPG, GIF, WEBP
- Возвращает:
  - Описание планировки (количество комнат, площадь, расположение)
  - Анализ удобства планировки (комнаты, санузлы, лифты)
  - Общую оценку удобства планировки (0-10)
  - Сильные и слабые стороны планировки
  - Рекомендации по улучшению

### Парсинг сайтов (`/parse_demo`)
- Принимает URL сайта
- Использует Selenium для парсинга (скриншот, извлечение текста)
- Извлекает: title, h1, первый абзац, весь видимый текст
- Автоматически анализирует извлечённый контент и скриншот

### История (`/history`)
- Хранит последние 10 запросов
- Сохраняет тип запроса, краткое описание, время

## 🛠️ Технологии

- **Backend**: FastAPI, Python 3.9+
- **AI**: OpenAI GPT-4o-mini (или GPT-4.1)
- **Frontend**: Vanilla JS, CSS3
- **Парсинг**: Selenium, BeautifulSoup4, httpx
- **Десктопное приложение**: PyQt5
- **Валидация**: Pydantic

## 📖 API Документация

После запуска сервера доступна интерактивная документация:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

Подробная документация API в файле [docs.md](docs.md)

## ⚠️ Требования

- Python 3.9+
- OpenAI API ключ с доступом к GPT-4o-mini или GPT-4.1
- Интернет-соединение для работы AI и парсинга
- ChromeDriver (для парсинга сайтов) - находится в папке `chromedriver-win64/`
- Для десктопного приложения: PyQt5 (устанавливается автоматически через `buildintel_app/УСТАНОВИТЬ_ЗАВИСИМОСТИ.bat`)
- ChromeDriver (для парсинга сайтов) - находится в папке `chromedriver-win64/`
- Для десктопного приложения: PyQt5 (устанавливается автоматически)

## 📝 Лицензия

MIT License

## 📤 Публикация на GitHub

Инструкция по публикации проекта на GitHub находится в файле [GITHUB_PUBLISH.md](GITHUB_PUBLISH.md)
#   b u i l d i n t e l  
 