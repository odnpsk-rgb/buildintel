# 🏗️ BuildIntel - AI Ассистент для строительства

Приложение для анализа маркетинга в строительстве с поддержкой мультимодальности (текст и изображения).

![Python](https://img.shields.io/badge/Python-3.9+-blue.svg)
![FastAPI](https://img.shields.io/badge/FastAPI-0.104+-green.svg)
![OpenAI](https://img.shields.io/badge/OpenAI-GPT--4o--mini-orange.svg)

## 🎯 Возможности

- **Анализ продающих текстов** в строительстве с точки зрения маркетинга
- **Анализ планировок квартир** с оценкой удобства, расположения комнат, санузлов и лифтов
- **Парсинг веб-страниц** с автоматическим анализом контента через Selenium
- **История запросов** - сохранение последних 10 запросов
- **Веб-интерфейс** - удобный UI для работы с приложением
- **Десктопное приложение** - PyQt5 приложение с полным функционалом

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Перейдите в директорию проекта
cd monitor_kp

# Создайте виртуальное окружение
python -m venv venv

# Активация виртуального окружения
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта на основе `.env.example`:

```env
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini
OPENAI_VISION_MODEL=gpt-4o-mini

# Опционально: Proxy для OpenAI (если требуется)
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
```

### 3. Запуск приложения

**Вариант А: Веб-версия**

```bash
# Windows:
start.bat

# Linux/Mac:
python -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```

Откройте браузер: http://localhost:8000

**Вариант Б: Десктопное приложение (PyQt5)**

1. Установите зависимости PyQt5:
```bash
cd buildintel_app
pip install -r requirements.txt
```

2. Запустите приложение:
```bash
# Дважды кликните на ЗАПУСТИТЬ.bat
# Или через терминал:
.\ЗАПУСТИТЬ.bat
```

3. Для создания .exe файла:
```bash
# Дважды кликните на build_exe_advanced.bat
```

Подробнее о десктопном приложении: см. `buildintel_app/README.md`

## 📁 Структура проекта

```
monitor_kp/
├── backend/                     # Backend модуль (FastAPI)
│   ├── __init__.py
│   ├── main.py                  # Главный файл FastAPI
│   ├── config.py                # Конфигурация приложения
│   │
│   ├── models/                  # Pydantic модели
│   │   ├── __init__.py
│   │   └── schemas.py           # Схемы запросов/ответов
│   │
│   └── services/                # Бизнес-логика
│       ├── __init__.py
│       ├── openai_service.py    # Интеграция с OpenAI
│       ├── parser_service.py    # Парсинг веб-страниц (Selenium)
│       └── history_service.py   # Управление историей запросов
│
├── frontend/                    # Frontend (HTML + JS)
│   ├── index.html              # Главная страница
│   ├── app.js                  # JavaScript логика
│   ├── styles.css              # Стили
│   └── 123.jpg                 # Фоновое изображение
│
├── buildintel_app/              # Десктопное приложение (PyQt5)
│   ├── main.py                 # Точка входа
│   ├── api_client.py            # Клиент для работы с API
│   ├── ui/                      # UI модули
│   │   ├── main_window.py       # Главное окно
│   │   ├── text_tab.py          # Вкладка анализа текста
│   │   ├── image_tab.py         # Вкладка анализа планировок
│   │   ├── parse_tab.py         # Вкладка парсинга URL
│   │   └── history_tab.py       # Вкладка истории запросов
│   ├── build_exe_advanced.bat   # Создание .exe файла
│   ├── ЗАПУСТИТЬ.bat            # Запуск приложения
│   └── README.md                # Документация десктопного приложения
├── chromedriver-win64/          # ChromeDriver для парсинга
│   └── chromedriver.exe
├── .env.example                 # Пример файла переменных окружения
├── .gitignore                   # Git ignore правила
├── requirements.txt             # Python зависимости
├── start.bat                    # Скрипт запуска для Windows
├── README.md                    # Этот файл
└── docs.md                      # Подробная документация API
```

## 🔧 Технологии

- **Backend:** FastAPI, Python 3.9+
- **AI:** OpenAI GPT-4o-mini (текст и изображения)
- **Парсинг:** Selenium, BeautifulSoup4
- **Frontend:** HTML, CSS, JavaScript (vanilla)
- **Desktop:** PyQt5
- **Упаковка:** PyInstaller

## 📚 Документация

- **API документация:** После запуска сервера доступна по адресу:
  - Swagger UI: http://localhost:8000/docs
  - ReDoc: http://localhost:8000/redoc
- **Подробная документация:** См. файл `docs.md`
- **Документация десктопного приложения:** См. `buildintel_app/README.md`

## 🎨 Основные функции

### Анализ продающих текстов
Анализ текстов конкурентов в строительстве с точки зрения маркетинга:
- Сильные и слабые стороны текста
- Уникальные торговые предложения (УТП)
- Рекомендации по улучшению
- Оценка эффективности продающих формулировок

### Анализ планировок квартир
Детальный анализ планировок с оценкой:
- Удобства расположения комнат
- Доступности и удобства санузлов
- Расположения и удобства лифтов
- Функциональности использования пространства
- Сильных и слабых сторон планировки

### Парсинг веб-страниц
Автоматический парсинг веб-страниц с:
- Скриншотом страницы
- Извлечением текстового контента
- Анализом через AI (текст + изображение)

## 🔐 Безопасность

- Файл `.env` с API ключами не попадает в Git (благодаря `.gitignore`)
- Все API ключи хранятся в переменных окружения
- CORS настроен для безопасной работы с фронтендом

## 📦 Зависимости

Основные зависимости указаны в `requirements.txt`:
- Для веб-версии: FastAPI, uvicorn, openai, selenium, webdriver-manager
- Для десктопного приложения: PyQt5 (устанавливается автоматически)

## 📤 Публикация на GitHub

Для публикации проекта на GitHub:

1. Создайте репозиторий на https://github.com
2. Выполните команды:
```bash
git init
git add .
git commit -m "Initial commit: BuildIntel"
git remote add origin https://github.com/YOUR_USERNAME/buildintel.git
git branch -M main
git push -u origin main
```

**Важно:** Файл `.env` с API ключами не будет опубликован благодаря `.gitignore`

## 📝 Лицензия

MIT License
