# BuildIntel Desktop Application

Десктопное приложение BuildIntel на PyQt5, повторяющее функционал веб-версии.

## 🎯 Создание .exe файла (запуск без терминала)

### Для создания .exe файла:

1. Откройте папку `buildintel_app` в проводнике Windows
2. **Дважды кликните** на файл **`build_exe_advanced.bat`** (рекомендуется) или `СОЗДАТЬ_EXE.bat`
3. Дождитесь завершения сборки (10-15 минут)
4. После сборки откроется папка `dist` с файлом `BuildIntel.exe`
5. **Дважды кликните** на `BuildIntel.exe` - приложение запустится БЕЗ терминала!

**Важно:** Перед запуском убедитесь, что бэкенд FastAPI запущен на http://localhost:8000

## Быстрый запуск через .bat (показывает терминал)

### ✅ ПРАВИЛЬНЫЙ СПОСОБ:

1. Откройте папку `buildintel_app` в проводнике Windows
2. **Дважды кликните** на файл **`ЗАПУСТИТЬ.bat`** (или `Запустить_BuildIntel.bat`, `START.bat`)
3. Файл автоматически проверит и установит все зависимости
4. Приложение запустится автоматически

**Важно:** Перед запуском убедитесь, что бэкенд FastAPI запущен на http://localhost:8000


### ⭐ Вариант 2: Запуск через .exe файл (БЕЗ терминала) - РЕКОМЕНДУЕТСЯ

**Создание .exe файла:**

**Если при запуске .exe была ошибка "ModuleNotFoundError: No module named 'PyQt5'":**

Используйте улучшенный скрипт сборки:

1. Откройте папку `buildintel_app` в проводнике Windows
2. **Дважды кликните** на файл **`build_exe_advanced.bat`** (включает все модули PyQt5)
3. Дождитесь завершения сборки (10-15 минут)
4. После сборки автоматически откроется папка `dist` с файлом `BuildIntel.exe`

**Обычная сборка (если ошибок не было):**
- Используйте `СОЗДАТЬ_EXE.bat` или `build.bat`

**Запуск .exe файла:**

1. Найдите файл `dist\BuildIntel.exe`
2. **Дважды кликните** на него
3. Приложение запустится **БЕЗ терминала**!

**Важно:** 
- Перед запуском убедитесь, что бэкенд FastAPI запущен на http://localhost:8000
- `.exe` файл можно переместить в любое место и запускать оттуда
- После создания `.exe` больше не нужны Python и зависимости

**Подробнее:** См. файл `КАК_СОЗДАТЬ_EXE.md`

## Возможности

- **Анализ продающих текстов** в строительстве
- **Анализ планировок квартир** с оценкой удобства
- **Парсинг сайтов** конкурентов с автоматическим анализом
- **История запросов** (последние 10 запросов)

## Требования

- Python 3.9+
- PyQt5
- Запущенный бэкенд FastAPI (http://localhost:8000)

## Установка

1. Установите зависимости (глобально или в venv):
```bash
# Автоматическая установка:
.\УСТАНОВИТЬ_ЗАВИСИМОСТИ.bat

# Или вручную:
pip install -r requirements.txt

# Или в виртуальное окружение:
python -m venv venv
venv\Scripts\activate  # Windows
pip install -r requirements.txt
```

**Примечание:** Если возникает ошибка "ModuleNotFoundError", дважды кликните на `УСТАНОВИТЬ_ЗАВИСИМОСТИ.bat`

2. **Запустите бэкенд FastAPI** (в отдельном терминале):
```bash
# Перейдите в корневую директорию проекта
cd ..

# Активируйте виртуальное окружение (если используете)
venv\Scripts\activate

# Запустите бэкенд
python -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```

   **Важно:** 
   - Бэкенд должен работать постоянно, пока вы используете приложение
   - Проверьте, что сервер запустился: откройте http://localhost:8000/docs в браузере
   - Должна открыться страница Swagger UI с документацией API

3. **Запустите десктопное приложение** (в новом терминале):
```bash
# Убедитесь, что вы в директории buildintel_app
cd buildintel_app

# Если используете venv - активируйте его:
# ..\venv\Scripts\activate  # или Activate.ps1 для PowerShell

# Запустите приложение
python main.py
```

   **Важно:** 
   - Если PyQt5 установлен **глобально** - можно запускать без активации venv
   - Если PyQt5 установлен **только в venv** - нужно активировать venv перед запуском
   - После запуска должно открыться окно приложения BuildIntel с вкладками

**Подробная инструкция:** См. файл [INSTALL.md](INSTALL.md) для детальных пошаговых инструкций.

## Сборка в .exe файл

### Автоматическая сборка (Windows):

**ВАЖНО:** Запускайте `.bat` файл напрямую, НЕ через Python!

**Способ 1: Двойной клик**
- Откройте папку `buildintel_app` в проводнике
- Дважды кликните на файл `build.bat`

**Способ 2: Через терминал**
```powershell
# В PowerShell:
.\build.bat

# Или в Command Prompt:
build.bat
```

**НЕ делайте так:** `python build.bat` ❌ (это вызовет ошибку!)

### Ручная сборка:
```bash
pip install pyinstaller
pyinstaller --name=BuildIntel --onefile --windowed --add-data="ui;ui" --hidden-import=PyQt6.QtCore --hidden-import=PyQt6.QtGui --hidden-import=PyQt6.QtWidgets --collect-all=PyQt6 --clean --noconfirm main.py
```

Исполняемый файл будет находиться в папке `dist/BuildIntel.exe`

## Структура проекта

```
buildintel_app/
├── main.py              # Точка входа
├── api_client.py        # Клиент для работы с API
├── ui/                  # UI модули
│   ├── main_window.py   # Главное окно
│   ├── text_tab.py      # Вкладка анализа текста
│   ├── image_tab.py     # Вкладка анализа планировок
│   ├── parse_tab.py     # Вкладка парсинга
│   └── history_tab.py   # Вкладка истории
├── requirements.txt     # Зависимости
├── build.bat           # Скрипт сборки для Windows
└── README.md           # Документация
```

## Устранение проблем

Если при запуске возникает ошибка "ModuleNotFoundError: No module named 'PyQt5'":

1. **Быстрое решение:** Дважды кликните на **`УСТАНОВИТЬ_ЗАВИСИМОСТИ.bat`**

2. **Вручную:**
```bash
pip install PyQt5==5.15.10 requests==2.31.0
```

Подробнее см. [INSTALL.md](INSTALL.md)

## Примечания

- Приложение требует запущенный бэкенд FastAPI на http://localhost:8000
- История сохраняется в файл `history.json` в текущей директории
- Для работы парсинга сайтов требуется ChromeDriver (как в веб-версии)
- При проблемах с PyQt6 см. раздел "Устранение проблем" выше
